# =============================================================================
# NextAuth Configuration
# =============================================================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-a-secret-key-min-32-chars-long-here

# =============================================================================
# Cognito Configuration (OIDC)
# =============================================================================
# These values are output from Terraform after deployment
COGNITO_CLIENT_ID=your-cognito-user-pool-client-id
COGNITO_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_XXXXXXXXX
# Note: Domain only (no https:// prefix) - the code adds the protocol
COGNITO_DOMAIN=your-app-domain.auth.us-east-1.amazoncognito.com

# Optional: Client secret (only needed if User Pool Client is configured with secret)
# COGNITO_CLIENT_SECRET=

# =============================================================================
# IDC OIDC Token Exchange Configuration (Primary Method)
# =============================================================================
# IAM Role ARN for exchanging IDC OIDC tokens for AWS credentials
# This preserves IDC identity through to S3 Access Grants (DIRECTORY_USER grants work!)
IDC_TOKEN_EXCHANGE_ROLE_ARN=arn:aws:iam::123456789012:role/your-project-env-idc-token-exchange

# =============================================================================
# Cognito Identity Pool for AWS Credentials (Legacy Fallback)
# =============================================================================
# Optional: Only needed if IDC token exchange is not configured
COGNITO_IDENTITY_POOL_ID=us-east-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX

# =============================================================================
# S3 Access Grants Configuration
# =============================================================================
S3_USER_DATA_BUCKET=your-user-data-bucket-name
S3_ACCESS_GRANTS_INSTANCE_ARN=arn:aws:s3:us-east-1:123456789012:access-grants/default
AWS_ACCOUNT_ID=123456789012
AWS_REGION=us-east-1

# =============================================================================
# API Configuration
# =============================================================================
API_BASE_URL=https://your-api-gateway-id.execute-api.us-east-1.amazonaws.com/prod
